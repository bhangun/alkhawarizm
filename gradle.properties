# Aljabr Engine Feature Flags
# Comma-separated list of backends to include: cpu, cuda, metal
alkhawarizm.backend=cpu,metal
# Strategy for backend selection: fallback, strict
alkhawarizm.strategy=fallback
# Enable training APIs (Autograd, ML, Train)
alkhawarizm.training=false

# Optimization Flags
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Babylon JDK (required for backend:hat which uses jdk.incubator.code)
# Points to the custom Babylon build that includes the HAT/code incubator module
org.gradle.java.installations.paths=/Users/bhangun/Workspace/workkayys/Products/Wayang/wayang-platform/3rdparty/babylon-hat-tensors-v2/build/macosx-aarch64-server-release/jdk
